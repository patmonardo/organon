{
  "name": "@organon/core",
  "version": "0.1.0",
  "description": "Organon Graph Data Science Core Library",
  "scripts": {
    "build": "tsc",
    "b": "tsc",
    "clean": "rm -rf dist node_modules",
    "dev": "tsc --watch",
    "d": "tsc --watch",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:ui": "vitest --ui",
    "lint": "eslint . --cache",
    "format": "prettier --write \"**/*.{ts,js,md,json,yml,yaml}\" --ignore-path .gitignore"
  },
  "dependencies": {
    "@types/node": "^22.15.17",
    "@types/papaparse": "^5.3.16",
    "apache-arrow": "^20.0.0",
    "js-yaml": "^4.1.0",
    "papaparse": "^5.5.3"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "vitest": "^3.1.3"
  }
}
