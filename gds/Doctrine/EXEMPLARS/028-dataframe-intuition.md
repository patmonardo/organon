# Exemplar 028 — DataFrame as Intuition

**Source file**: `gds/examples/collections_dataframe_intuition.rs`
**Arc position**: Beginning: DataFrame as Intuition, before Essence mediation
**Prior exemplar**: [027 — Model:Feature::Plan as Essence Middle](027-model-feature-plan-middle.md)
**Next exemplar**: TBD

---

## Principle

The first moment of the Dataset pipeline is the DataFrame itself. Raw contents become available to the system when they are gathered into a tabular body. This is Intuition: not yet Essence, not yet Semantics, but already formed enough to appear.

---

## What This Example Does

It constructs the smallest first-moment passage:

1. Creates a DataFrame from raw rows
2. Prints the immediate Frame, Series, and dtype body
3. Wraps that body as a named `Dataset`
4. Builds a `Plan` from a DataFrame `Expr`
5. Wraps the Plan as a `Feature`
6. Prepares a `Model` stamp over the DataFrame body
7. Prints the mapping from Beginning into Essence

The point is not runtime complexity. The point is location: the DataFrame is the first object of the pipeline.

---

## The Arc Reading

```text
Raw contents
  -> DataFrame                      [Intuition: tabular consciousness]
  -> Frame:Series::Expr             [Being: immediate body]
  -> Model:Feature::Plan            [Essence: mediated body]
  -> Corpus:Language::Semantics     [Concept: synthetic return]
```

The DataFrame is the beginning that `Model:Feature::Plan` reflects. It is not bypassed by Doctrine. Doctrine begins where the system first has a formed body.

---

## The Mapping

The exemplar locks the first-to-second movement:

| Beginning | Essence | Concept |
|---|---|---|
| `Frame` | `Model` | `Corpus` |
| `Series` | `Feature` | `Language` |
| `Expr` | `Plan` | `Semantics` |

This is the simplified pipeline:

```text
DataFrame
  -> Model:Feature::Plan
  -> SemDataset
```

---

## Why This Follows Exemplar 027

Exemplar 026 showed the End. Exemplar 027 returned to the hard Middle. This exemplar completes the retrograde clarification by naming the Beginning.

Once the system sees `SemDataset` and then `Model:Feature::Plan`, it can finally say what they presuppose: the DataFrame as Intuition.

---

## Key Vocabulary

**`DataFrame`** — The first formed body of the Dataset pipeline. See [DataFrame as Intuition](../REFERENCES/collections-dataset/dataframe-intuition.md).

**`Frame:Series::Expr`** — The immediate DataFrame triad before Dataset mediation.

**`Model:Feature::Plan`** — The Essence triad that reflects DataFrame body into semantic form. See [Model:Feature::Plan](../REFERENCES/collections-dataset/model-feature-plan.md).

**`SemDataset`** — The Concept/end-view receiving Corpus, Language, and Semantics. See [Semantic Pipeline](../REFERENCES/collections-dataset/semantic-pipeline.md).

---

## Student Notes

- Do not treat the DataFrame as a mere implementation detail. It is the first formed appearance of contents.
- The DataFrame is still extensional. Essence begins only when Frame, Series, and Expr are reflected as Model, Feature, and Plan.
- This exemplar deliberately comes after 026 and 027 because the system has learned its Beginning from its Middle and End.
- In science terms: the Program begins as One, differentiates into the triadic Many, and returns as One. `form/program` is the return path through `collections/{dataframe,dataset}`.

---

## One -> Many -> One

```text
PureForm/Program
  -> DataFrame (Beginning)
  -> Model:Feature::Plan (Essence)
  -> Corpus:Language::Semantics (Concept)
  -> Programmatic Oneness (self-readable process)
```

This is why the cycle matters: only a full quadrant and return reveals the true placement of Source, Document, and Annotation under Corpus while preserving DataFrame as Corpus-being.

---

## What This Example Does Not Show

- Full Corpus construction
- LanguageModel fitting
- Logical-form parsing through `SemDataset`
- DirectCompute dispatch
