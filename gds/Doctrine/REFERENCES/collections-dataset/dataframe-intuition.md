# DataFrame as Intuition

`DataFrame` is the first moment of the Dataset Meta Pipeline. It is not a pre-semantic `Source` placeholder and not a recurrence mechanism. It is the first formed body: raw contents gathered into tabular intuition.

---

## Definition

The Beginning has three immediate terms:

| Term | Role |
|---|---|
| `Frame` | The whole tabular body in which contents first appear together |
| `Series` | A columnar line of appearance, still physical and extensional |
| `Expr` | A DataFrame operation, still executable rather than doctrinal |

This is the sphere of Being for the Dataset pipeline.

---

## Passage Into Essence

The first moment passes into the Middle by reflection:

```text
Frame  -> Model
Series -> Feature
Expr   -> Plan
```

The DataFrame is not discarded. It becomes readable.

```text
Frame:Series::Expr
becomes
Model:Feature::Plan
```

This is why `Model:Feature::Plan` is the sphere of Essence. It receives the immediate DataFrame body and mediates it into named, prepared, inspectable form.

---

## Passage Into Concept

The later synthetic reading is:

```text
Model   -> Corpus
Feature -> Language
Plan    -> Semantics
```

The Model has its body as Corpus. The Feature reflects that body as Language. The Plan comprehends Language as Semantics.

---

## Doctrine Rule

Do not search before the DataFrame for the first pipeline object.

The first moment of the Dataset pipeline is the DataFrame itself: Intuition as tabular consciousness, the immediate body in which raw contents become available for essential mediation.

---

## Science Requirement

For `form/program`, the pipeline satisfies the science requirement as a One -> Many -> One return:

```text
One   : PureForm with singular Feature = Program
Many  : triadic pipeline across collections/{dataframe,dataset}
	DataFrame -> Model:Feature::Plan -> Corpus:Language::Semantics
One   : Programmatic Form recovered as a single self-mediating process
```

This is the doctrinal reason the advanced client DSL may emerge from DataFrame primitives without leaving them behind: multiplicity is generated and then recollected into one Form-process.

---

## Review Rule

When adding a new Dataset pipeline entrypoint, ask:

1. Where does the raw content become a DataFrame body?
2. Which Frame, Series, and Expr are visible before Dataset mediation?
3. Which Model, Feature, and Plan receive those immediate terms?
4. Which Corpus, Language, and Semantics become possible as Concept?

If the first DataFrame body is hidden, the beginning of the pipeline is hidden.
