/**
 * A marker interface used to identify NodePropertyValues implementations
 * that represent filtered views of properties.
 * This is a direct translation of GDS's org.neo4j.gds.api.properties.nodes.FilteredNodePropertyValuesMarker.
 * In TypeScript, this might be used for type checking or as a generic constraint.
 */
export interface FilteredNodePropertyValuesMarker {
  // This interface is intentionally empty, serving only as a marker.
}
