{
  "name": "@organon/logic",
  "version": "0.1.0",
  "description": "Organon Logic Package",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:ui": "vitest --ui",
    "lint": "echo 'Linting not configured yet'"
  },
  "dependencies": {
    "@organon/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.1.3"
  }
}
