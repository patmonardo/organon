/**
 * Principle: Immediate that contains its Mediacy.
 * Being (Immediate) contains Essence (Mediacy) → Concept (movement).
 */
export type Principle<I, M> = {
  immediate: I; // the present, determinate
  mediacy: M;   // the contained conditions/grounds
};
