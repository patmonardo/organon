/**
 * Compatibility re-export.
 *
 * Tests and older imports expect the SemanticHydrator under `src/data/semantic-hydrator`.
 * The implementation lives under `src/execution/semantic-hydrator.ts`.
 */

export * from '../execution/semantic-hydrator';
