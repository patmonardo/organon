{
  "name": "@organon/reality",
  "version": "0.1.0",
  "description": "Organon Graph Data Science Reality Library",
  "scripts": {
    "build": "tsc",
    "b": "tsc",
    "clean": "rm -rf dist node_modules",
    "dev": "tsc --watch",
    "d": "tsc --watch",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:ui": "vitest --ui",
    "lint": "eslint . --cache",
    "format": "prettier --write \"**/*.{ts,js,md,json,yml,yaml}\" --ignore-path .gitignore"
  },
  "dependencies": {
    "@types/node": "^22.15.17"
  },
  "devDependencies": {
    "vitest": "^3.1.3"
  }
}
